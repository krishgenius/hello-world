packages:
  - package: dbt-labs/dbt_utils
    version: 1.2.0
  # use this format when accessing your repository via a github application token
  - git: "https://{{env_var('DBT_ENV_SECRET_GIT_CREDENTIAL')}}@github.com/krishgenius/macro_repo.git" # git HTTPS URL