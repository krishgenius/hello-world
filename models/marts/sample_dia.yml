version: 2

models:
  - name: sample_dia
    description: "this combines item, warehouse and inventory"
    columns:
      - name: skey
        description: "derived hash column on item and warehouse id"
      -name: Warehouse_name
       description: "Name of the warehouse and location"  
        tests:
          - unique
          - not_null
 